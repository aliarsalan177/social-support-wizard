# Copy to `.env` and fill in. `.env` is git-ignored.

# OpenAI API key — powers the Step 3 "Help me write" feature.
# Without it, the AI button is disabled but the rest of the form works.
VITE_OPENAI_API_KEY=

# Optional overrides:
VITE_OPENAI_MODEL=gpt-3.5-turbo
VITE_SUBMIT_URL=/api/applications
